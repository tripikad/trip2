language: php

php:
  - 5.6

sudo: false

install:
    - composer install --no-interaction --prefer-source
    - cp .env.travis .env
    - php artisan key:generate
    - chmod -R o+w storage/
    - chmod -R o+w public/images/
    - php artisan migrate

script: vendor/bin/phpunit